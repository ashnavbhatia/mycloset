<% was_validated = brand_name.errors.any? %>

<%= form_with(model: brand_name, local: true, novalidate: true, class: "mb-3") do |f| %>
  <%= f.button class: "btn btn-outline-secondary btn-block" %>
<% end %>
